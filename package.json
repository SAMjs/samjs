{
  "name": "samjs",
  "description": "",
  "version": "0.2.1",
  "repository": "https://github.com/SAMjs/samjs",
  "author": {
    "name": "Paul Pflugradt",
    "email": "paul.pflugradt@gmail.com"
  },
  "main": "lib/main.js",
  "engines": {
    "node": "*"
  },
  "dependencies": {
    "bluebird": "^3.1.1",
    "core-util-is": "^1.0.2",
    "debug": "^2.2.0",
    "socket.io": "^1.4.4"
  },
  "devDependencies": {
    "atom-ui-reporter": "0.0.1",
    "chai": "^3.4.1",
    "coffee-script": "^1.10.0",
    "mocha": "^2.3.4",
    "parallelshell": "git://github.com/keithamus/parallelshell#dev",
    "samjs-client": "*",
    "simple-reload": "0.0.4",
    "socket.io-client": "^1.4.4"
  },
  "keywords": [],
  "readmeFilename": "README.md",
  "scripts": {
    "patch": "npm version patch;npm publish;git push;git push --tags",
    "patchAll": "parallelshell 'cd ../samjs-client;git add -A .;git commit -m \"preparations for patch\"; npm version patch;npm publish;git push;git push --tags' 'cd ../samjs-mongo;git add -A .;git commit -m \"preparations for patch\"; npm version patch;npm publish;git push;git push --tags' 'cd ../samjs-mongo-client;git add -A .;git commit -m \"preparations for patch\"; npm version patch;npm publish;git push;git push --tags' 'cd ../samjs-mongo-auth;git add -A .;git commit -m \"preparations for patch\"; npm version patch;npm publish;git push;git push --tags' 'cd ../samjs-mongo-auth-client;git add -A .;git commit -m \"preparations for patch\"; npm version patch;npm publish;git push;git push --tags' 'cd ../samjs-mongo-deletion;git add -A .;git commit -m \"preparations for patch\"; npm version patch;npm publish;git push;git push --tags' 'cd ../samjs-mongo-deletion-client/;git add -A .;git commit -m \"preparations for patch\"; npm version patch;npm publish;git push;git push --tags' 'cd ../samjs-mongo-configwise/;git add -A .;git commit -m \"preparations for patch\"; npm version patch;npm publish;git push;git push --tags'"
  }
}
