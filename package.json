{
  "name": "samjs",
  "description": "",
  "version": "0.3.0",
  "repository": "https://github.com/SAMjs/samjs",
  "author": {
    "name": "Paul Pflugradt",
    "email": "paul.pflugradt@gmail.com"
  },
  "main": "lib/main.js",
  "engines": {
    "node": "*"
  },
  "files": [
    "lib/"
  ],
  "dependencies": {
    "bluebird": "^3.4.6",
    "core-util-is": "^1.0.2",
    "debug": "^2.2.0",
    "socket.io": "1.5.0"
  },
  "devDependencies": {
    "atom-ui-reporter": "0.0.1",
    "chai": "^3.5.0",
    "coffee-script": "^1.11.1",
    "mocha": "^3.1.2",
    "rimraf": "^2.5.4",
    "samjs-client": "^0.2.2",
    "simple-reload": "0.0.4",
    "socket.io-client": "1.5.0"
  },
  "keywords": [],
  "readmeFilename": "README.md",
  "scripts": {
    "build": "rimraf lib/ && coffee --no-header --compile --output lib/ src/*.coffee",
    "test": "mocha",
    "test:all": "mocha --timeout 1000 --compilers coffee:coffee-script/register testAll.coffee",
    "preversion": "npm test",
    "version": "npm run build && git add .",
    "postversion": "git push && git push --tags && npm publish"
  }
}
